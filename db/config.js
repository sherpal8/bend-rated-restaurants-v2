const dbConfig = {
  database: "manchester_restaurants",
  host: "localhost",
  port: 5432 // port pertains to 'pSQL' (blue-elephant)
  // username: 'username' // only for ubuntu user
  // passwor: 'password' // only for ubuntu user
};

module.exports = dbConfig;
